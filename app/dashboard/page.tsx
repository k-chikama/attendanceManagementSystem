import AppLayout from "@/components/layout/layout";

export default function Dashboard() {
  return (
    <AppLayout>
      {/* ここにダッシュボードのUIやロジックを記述。ユーザー情報が必要な場合はContextやカスタムフックで取得してください。 */}
      <div>ダッシュボード</div>
    </AppLayout>
  );
}
